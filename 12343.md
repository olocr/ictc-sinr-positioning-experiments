## 위치 추정 방법론

### 1. 기본 아이디어

실제 삼각측위(trilateration)는 3개 기지국까지의 **거리**를 알아야 한다. 그러나 SINR → 거리 변환은 interference, fading 등으로 인해 부정확하므로, 본 연구에서는 **SINR을 가중치로 활용하는 Weighted Centroid** 방식을 채택하였다.

---

### 2. SINR → Linear 변환

SINR은 dB 단위(로그 스케일)이므로 선형 스케일로 변환하여 가중치로 사용한다.

$$w_i = 10^{\frac{SINR_i}{10}}$$

| SINR (dB) | Linear 가중치 |
|---|---|
| 20 dB | 100 |
| 10 dB | 10 |
| 0 dB | 1 |
| -10 dB | 0.1 |

dB가 10 증가할수록 가중치가 10배 증가하므로, 신호 품질이 높은(가까운) 기지국에 강한 가중치가 부여된다.

---

### 3. Weighted Centroid 수식

$$\hat{x} = \frac{\sum_{i=1}^{N} w_i \cdot x_i}{\sum_{i=1}^{N} w_i}, \quad \hat{y} = \frac{\sum_{i=1}^{N} w_i \cdot y_i}{\sum_{i=1}^{N} w_i}$$

- $(\hat{x}, \hat{y})$: 추정 위치
- $(x_i, y_i)$: $i$번째 기지국 좌표
- $w_i$: $i$번째 기지국 SINR의 linear 변환값
- $N$: 사용된 기지국 수 (최대 6개, SINR ≥ -20 dB)

---

### 4. 기지국 배치

시뮬레이션 환경(scenario-three.cc) 기준 기지국 좌표는 다음과 같다.

| Cell ID | X (m) | Y (m) |
|---|---|---|
| 2 (center) | 2000 | 2000 |
| 3 | 3000 | 2000 |
| 4 | 2500 | 2866 |
| 5 | 1500 | 2866 |
| 6 | 1000 | 2000 |
| 7 | 1500 | 1134 |
| 8 | 2500 | 1134 |

중심(2000, 2000)에서 반경 1000m, 60도 간격으로 배치 (isd = 1000m, 3GPP UMi 구성).

---

### 5. 오차 평가 (RMSE)

$$RMSE = \sqrt{\frac{1}{M}\sum_{j=1}^{M}\left[(\hat{x}_j - x_j)^2 + (\hat{y}_j - y_j)^2\right]}$$

- $M$: 평가 UE 수 (46개)
- 시뮬레이션 영역: 4000 × 4000 m

| 지표 | 값 |
|---|---|
| 평균 오차 | 317 m |
| RMSE | 378 m |
| 최소 오차 | 12 m |
| 최대 오차 | 890 m |

---

### 6. 한계 및 향후 개선

Weighted Centroid 방식은 다음 경우에 오차가 크게 증가한다.

- **Cell edge UE**: 모든 기지국 SINR이 낮아 가중치 차이가 미미한 경우
- **지배적 셀 부재**: 여러 기지국의 SINR이 유사한 경우
- **SINR 왜곡**: Interference 및 NLOS 환경에서 SINR이 거리를 직접 반영하지 않는 경우

이를 해결하기 위해 향후 MLP 기반 딥러닝 모델을 적용하여 SINR 벡터로부터 UE 위치를 직접 학습하는 방식으로 개선할 예정이다.